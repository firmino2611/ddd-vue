import store from './../../store'
import LocalStorage from "./LocalStorage";
/**
 * Classe responsavél por fazer o tratamento
 * das permissões de acesso dos usuários
 */
export default class Acl {

  /**
   * Valida se o usuario tem as permissões necessárias.
   * Enviar as
   * @param {string} permission
   */
  static can(permission) {
    // todo: implemention here
  }
}
