import Vue from 'vue'
import Router from 'vue-router'
// import Test from '@/components/Test'
import index from '@/router/routes'

Vue.use(Router)
console.log(index)

export default new Router(index)
